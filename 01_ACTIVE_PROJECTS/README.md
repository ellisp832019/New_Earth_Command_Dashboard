# Active Projects

This folder is the shared active-project workspace used by the repo and the Obsidian vault mirror.

It is here on purpose so dashboard-aligned notes, build logs, indexes, and sync outputs have a stable home without being mixed into Flutter app source.

Current project:

- `00_NEW_EARTH_DASHBOARD`
