# Codex Prompt

Upgrade and integrate this New Earth Repo Intelligence Bridge into the current project.

Rules:
- Keep everything local-first.
- Do not delete files.
- Do not modify project source code unless I explicitly approve.
- Keep Obsidian notes safe by only updating content between AUTO-GENERATED markers.
- Keep Dashboard JSON exports simple and stable.
- Add any missing tests or validation scripts.

Tasks:
1. Check the project profile in `/profiles` and update `repo_root` for this repo.
2. Run `scripts/validate_config.ps1`.
3. Run `scripts/sync_all.ps1`.
4. Confirm Markdown exports are created in the Obsidian vault.
5. Confirm JSON exports are created in the Dashboard bridge folder.
6. Add Dashboard reader code only if this repo is the New Earth Dashboard.
7. Do not connect any AI write actions yet; read-only context first.
