# Example Repo Research Report

This is a placeholder example showing where generated reports will live.

Run:

```bash
python scripts/run_research.py --repo PATH_TO_REPO --profile profiles/microgrow.profile.json --out reports
```
