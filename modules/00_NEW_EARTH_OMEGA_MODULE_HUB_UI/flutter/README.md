# Flutter Module Hub Scaffold

Copy or merge the contents of `lib/` into the New Earth Dashboard Flutter app.

The code is intentionally lightweight and backend-free. It uses mock module data so the UI can be built before real module services exist.
