# App Function Reference

This guide describes the current New Earth Command Dashboard app functions and how to use them.

It is organized by area and covers the available screens, actions, and workflows in the current version.

---

## Navigation Reference

The app is organized into the following primary areas:

- `Dashboard`
- `Projects`
- `Tasks`
- `Planner`
- `More`

Under `More`, the current supporting areas are:

- `Journal`
- `Learning`
- `Content`
- `Business`
- `Wellbeing`
- `Inbox`
- `Voice Assistant`
- `Settings`

### Route names and screens

- `/dashboard` — Dashboard screen
- `/projects` — Projects list screen
- `/projects/new` — Add project screen
- `/projects/:projectId` — Project detail screen
- `/projects/:projectId/edit` — Edit project screen
- `/tasks` — Tasks list screen
- `/tasks/new` — Add task screen
- `/tasks/:taskId/edit` — Edit task screen
- `/planner` — Planner screen
- `/more` — More screen
- `/journal` — Journal list screen
- `/journal/new` — Add journal entry screen
- `/journal/:journalEntryId/edit` — Edit journal entry screen
- `/learning` — Learning list screen
- `/learning/new` — Add learning item screen
- `/learning/:learningItemId/edit` — Edit learning item screen
- `/content` — Content list screen
- `/content/new` — Add content item screen
- `/content/:contentItemId/edit` — Edit content item screen
- `/business` — Business opportunities screen
- `/business/new` — Add business opportunity screen
- `/business/:businessId/edit` — Edit business opportunity screen
- `/wellbeing` — Wellbeing screen
- `/wellbeing/new` — Add wellbeing checkin screen
- `/inbox` — Inbox list screen
- `/inbox/new` — Add inbox item screen
- `/settings` — Settings screen
- `/voice-assistant` — Voice Assistant screen

---

## Dashboard

### Purpose

The Dashboard is the app home screen. It is designed to give you rapid orientation for the day.

### What it shows

- Today's focus, intention, and plan status
- Top 3 tasks or priorities
- Quick access to create content, tasks, notes, or support items
- Progress and summary cards for key areas

### How to use it

1. Open the app and start on `Dashboard`.
2. Review the current focus and any existing top tasks.
3. Use quick capture or card shortcuts to create a new item if needed.
4. Tap into the planner, tasks, or projects when you want to move from overview to action.

### Best practice

- Use the Dashboard to confirm the day’s highest priority direction.
- Avoid using it as the only place to manage work; it is a navigation surface and orientation tool.

---

## Projects

### Purpose

The `Projects` screen holds work at a larger scale.

### What it does

- Lists active projects
- Allows adding a new project
- Lets you open project detail pages

### Key functions

- Add project: `Projects` → add button → enter project data
- View project: tap a project card
- Edit project: from the project detail page
- Create a task for a project: from the project detail page, use the task creation flow
- Create a project from Voice Assistant: review the project suggestion, edit the project status/priority/vision fields, then save locally

### How to use it

1. Open `Projects`.
2. Scan the existing project list.
3. Add new projects only when the work needs a larger container.
4. Open a project to see its details and related tasks.

### Project detail screen

The project detail screen provides:

- project purpose and current status
- progress or goal details
- linked tasks
- linked journal entries
- linked learning items
- linked content ideas
- linked business opportunities
- project-aware shortcuts for adding new linked items
- quick navigation back to the dashboard

---

## Tasks

### Purpose

Tasks are where work becomes concrete.

### What it does

- Lists task items across projects
- Supports task creation and editing
- Allows promoting tasks to `Top 3`
- Supports moving tasks into today or marking them done

### Key functions

- Add task: `Tasks` → add button → enter task fields
- Edit task: tap a task card
- Mark Top 3: use the top task action on a task card
- Change status: update the task status in the edit form
- Filter and sort: use available controls to focus the task list

### How to use it

1. Open `Tasks`.
2. Capture work as task items with just enough detail.
3. Assign tasks to a project when appropriate.
4. Promote three essential tasks to `Top 3`.
5. Mark tasks done as you complete them.

### Task status guidance

- `Inbox`: captured work not yet scheduled.
- `Today`: work planned for the current day.
- `In Progress`: work underway.
- `Blocked`: waiting on something.
- `Done`: complete.
- `Parked`: intentionally paused.

---

## Planner

### Purpose

The `Planner` screen is the daily planning and review area.

### What it does

- Shows the daily plan for today
- Displays the Top 3 tasks in the plan
- Offers fields for focus, intention, and review

### How to use it

1. Open `Planner`.
2. Confirm the date and the daily plan state.
3. Check the Top 3 tasks and align them with today’s work.
4. Use the planner for end-of-day review when available.

### Best practice

- Use Planner as the place to close the day and make the next day clearer.
- Keep the plan simple and actionable.

---

## More

### Purpose

`More` is the supporting area with modular flows for longer-term work and reflection.

### What it includes

- `Journal`
- `Learning`
- `Content`
- `Business`
- `Wellbeing`
- `Inbox`
- `Voice Assistant`
- `Settings`

### How to use it

1. Open `More`.
2. Choose the support area you need:
   - `Journal` for reflective notes
   - `Learning` for learning ideas
   - `Content` for content planning
   - `Business` for opportunities and leads
   - `Wellbeing` for daily checkins
   - `Inbox` for loosely captured items
   - `Voice Assistant` for voice-driven input

---

## Business

### Purpose

The Business screen is for tracking opportunities, partnerships, and leads in a calm, local-first way.

### What it does

- Lists saved business opportunities.
- Lets you add a new opportunity with project, type, status, next step, and follow-up details.
- Lets you edit an existing opportunity and save updates locally.

### Current behavior

- Add an opportunity from the Business screen using the top-right add button.
- Tap any opportunity card to open the edit screen.
- The edit form preloads saved values and returns to the Business list after save.

### Notes

- Business data is stored locally and is available on the current device.
- Opportunity entries can include company/contact, deadlines, follow-up dates, document references, and notes.

   - `Settings` for app options

---

## Journal

### Purpose

Journal is for daily reflection and progress notes.

### What it does

- Lists journal entries
- Allows adding a new entry
- Supports editing an existing entry

### How to use it

1. Open `Journal`.
2. Tap the add button to create a new entry.
3. Write the entry and save.
4. Edit entries later if needed.

---

## Learning

### Purpose

Learning captures study ideas, books, courses, or research topics.

### What it does

- Lists learning items
- Allows adding new learning items
- Supports editing existing items

### How to use it

1. Open `Learning`.
2. Add a new learning item for a skill, topic, or resource.
3. Edit it as your plan or understanding changes.

---

## Content

### Purpose

Content is for planning content ideas, drafts, and publication work.

### What it does

- Lists content items
- Allows adding new content items
- Supports editing content items

### How to use it

1. Open `Content`.
2. Create a content item for an idea, article, or story.
3. Use the content list to keep ideas organized.

---

## Business

### Purpose

The `Business` area is for opportunities, partnerships, leads, and commercial work.

### What it does

- Lists business opportunities
- Allows adding a new opportunity
- Supports editing an opportunity

### Add business opportunity flow

1. Open `Business`.
2. Tap the add button.
3. Fill fields such as name, project, type, and status.
4. Optionally add company/contact, next step, deadline, follow-up date, document link, and notes.
5. Save.
6. Confirm the item appears in the list.

### Edit business opportunity flow

1. Tap a business opportunity card.
2. Confirm the screen title is `Edit Opportunity`.
3. Change fields as needed.
4. Save.
5. Confirm the updated values appear in the list.

### Current important behavior

- The add/edit business screen shares the same form for create and edit.
- Business types follow the FSD set: `Job`, `Contract`, `Grant`, `Partnership`, `Client`, `Funding`, `Mentor`, `Investor`, `Collaboration`, and `Business Idea`.
- Business statuses follow the FSD set: `Researching`, `Preparing`, `Applied`, `Waiting`, `Follow-up Needed`, `Accepted`, `Rejected`, `Paused`, and `Archived`.
- The screen is strong for tracking follow-up action and deadlines.

---

## Wellbeing

### Purpose

Wellbeing is for short daily checkins and status tracking.

### What it does

- Shows the wellbeing area
- Allows adding a wellbeing checkin

### How to use it

1. Open `Wellbeing`.
2. Tap `New` if available.
3. Enter the checkin details and save.

---

## Inbox

### Purpose

Inbox is the capture area for ideas, thoughts, and unprocessed items.

### What it does

- Lists inbox items
- Allows adding a new inbox item
- Lets you park items for later
- Lets you convert items into Tasks, Journal Entries, Content Ideas, Learning Items, or Business Opportunities

### How to use it

1. Open `Inbox`.
2. Add a new item quickly.
3. Park or convert items when you are ready to process them.
4. Use Inbox as the staging ground for later processing.

---

## Voice Assistant

### Purpose

The voice assistant screen supports reviewed voice capture for local dashboard actions.

For a step-by-step walkthrough, see [Voice Assistant Guide](voice_assistant_guide.md).

### How to use it

1. Open `Voice Assistant`.
2. Press `Start Listening`, speak, then press `Stop` when finished.
3. Review the suggested type, title, and project if the transcript clearly points to one.
4. Choose the destination: Task, Journal Entry, Content Idea, Business Opportunity, Idea, or Codex Prompt.
5. Optionally adjust the project link.
6. Save it into the local dashboard data, or prepare a Codex prompt for manual review.

### Current behavior

- Voice Capture can be opened from `Dashboard` Quick Capture or from `More`.
- On Windows, Gaia waits for a connected headset or headset microphone before the app fully opens.
- While that headset gate is open, Gaia keeps rechecking automatically so a newly connected headset can unlock the app without a restart.
- On Windows, Gaia gives a short startup greeting once voice replies are ready, so you know the assistant is alive before you speak.
- Microphone capture is press-to-listen only; there is no always-listening background mode.
- On Windows, `Start Listening` first tries the local desktop speech bridge for one-shot Whisper transcription before falling back to the system dictation path.
- Paste Transcript and mock transcript remain available as fallbacks.
- The `Voice Starter Deck` gives you quick paths for Build Day, Task, Journal, Content, Business, Codex, and Idea captures.
- The `Voice Starter Deck` now includes a `Project` starter for shaping a new project record before you save it.
- The `Voice Starter Deck` also includes `Summarize Today` and `What's Next` macros for review-first planning.
- The `Action Macros` card gives you instant moves like `Start Build Day`, `Plan Day`, `Recall Memory`, `Summarize Today`, `What's Next`, and `Continue Thread`.
- The `Assistant Reply` card gives a short plain-language read on what the app thinks you said before you save it.
- The `Voice Briefing` card turns the current command into a numbered suggested sequence of next steps.
- The `Current Thread` card keeps a running voice conversation in view so you can continue it or start fresh.
- `Wizard Mode` turns the assistant into a step-by-step conversation that assembles the draft from one answer at a time.
- Gaia now keeps a handsfree wake listener armed while the app is open on Windows, so saying `Hey Gaia` can bring up the Voice Assistant without pressing Start Listening first.
- If the wake route is still settling, Gaia can also surface a small conversation dock on the dashboard with the captured transcript and next step, speak that captured reply through the configured voice output, and offer quick follow-up chips that reopen the assistant with a preselected intent.
- The wake layer, dashboard dock, and full Voice Assistant share one session state machine so only one voice path owns listening or speaking at a time.
- `Settings` now includes voice output controls for assistant speech, voice selection, speech rate, and preview.
- The `Command Router` surfaces the next likely move, such as opening Tasks, Planner, Dashboard, Projects, Inbox, Journal, Content, or Business, and can offer `Continue Thread` when there is remembered voice context.
- The screen suggests a command type, cleaned title, and related project using local parsing rules.
- The screen can also suggest a new project capture and expose editable project status, priority, vision, and next action fields before save.
- The screen can recognize wake phrases such as `Hey Gaia` before the command and strip them out so the rest of the sentence is parsed normally.
- The command router can surface summary and next-step macros to make the assistant feel more conversational.
- The screen also extracts structured details where it can, such as task category/priority, content platform/type, journal next actions, and business contact/status hints.
- Those extracted details are editable before save, so you can correct the parser when the wording is close but not perfect.
- Task transcripts save as Inbox tasks.
- Journal transcripts save as Reflection entries.
- Idea transcripts save into Inbox as Future Ideas.
- Content transcripts save as Content Ideas.
- Business transcripts save as Business Ideas.
- Codex prompts are copied/reviewed manually and are not executed by the app.
- Speech recognition uses the device/platform speech service provided through the app's speech-to-text package where stable; saved dashboard data remains local.
- On Windows, `Start Listening` opens native Windows voice typing inside the transcript field so the app can stay stable without the beta recognizer.
- On Windows, the app also keeps a lightweight handsfree wake listener armed while Gaia is open so `Hey Gaia` can open the Voice Assistant without a button press.
- On Windows, assistant replies and briefings can speak back through the locally installed system voices selected in `Settings`.
- Starting voice typing on Windows now preserves the current window mode instead of forcing the app out of fullscreen.
- The structured review panel stays editable before you save the capture.
- `Paste Transcript` and mock capture still remain available there as fallbacks.

---

## Settings

### Purpose

Settings let you control app preferences.

### What it does

- Adjust theme mode
- Manage any available app-level options

### How to use it

1. Open `Settings`.
2. Review the available options.
3. Change preferences as needed.

---

## Data and storage behavior

### Local-first

The current app stores data locally in SQLite. There is no cloud sync or login required.

### Persistence

- Items are saved immediately when created or updated.
- The app is designed to work offline.

---

## Recommended use patterns

### Start with clarity

1. Open `Dashboard`.
2. Confirm the highest priority focus.
3. Use `Tasks` and `Planner` to turn that focus into concrete work.

### Use `Projects` for context

- Put work into project containers when it is part of a larger mission.
- Keep `Tasks` as the concrete action layer.

### Keep the system calm

- Use `Inbox` for raw capture.
- Process inbox items later into Tasks, Projects, or supporting areas.
- Use `Business` for opportunities rather than general to-do items.

### Keep reviews simple

- The `Planner` screen is the place for daily review.
- Do not make the Planner a second task list.

---

## Summary of every function

This app supports these main functional groups:

- dashboard orientation
- project structure
- task action management
- daily planning
- journal reflection
- learning tracking
- content planning
- business opportunity tracking
- wellbeing checkins
- inbox capture
- voice-assisted input
- basic settings

If you want, I can also add a second page that maps these functions to the exact UI controls and key buttons in the current build.
